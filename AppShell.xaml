<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="mqtttool.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:mqtttool"
    Shell.FlyoutBehavior="Disabled"
    Title="mqtttool">

    <!--<TabBar>
        <Tab Title="数据">
            <ShellContent ContentTemplate="{DataTemplate local:Data}"/>
        </Tab>
        <Tab  Title="Home" >
            <ShellContent 
        ContentTemplate="{DataTemplate local:MainPage}"
       
        />
        </Tab>
        <Tab Title="About">
            <ShellContent 
        ContentTemplate="{DataTemplate local:About}"
        />
        </Tab>

    </TabBar>-->
    
    <ShellContent ContentTemplate="{DataTemplate local:MainPage}" Title="Home" Route="MainPage"/>
    <ShellContent ContentTemplate="{DataTemplate local:Data} " Title="Data" Route="Data"/>
    <ShellContent ContentTemplate="{DataTemplate local:About}" Title="About" Route="About"/>
    <ShellContent ContentTemplate="{DataTemplate local:TCP}" Title="TCP" Route="TCP"/>

</Shell>
